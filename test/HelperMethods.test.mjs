import { expect } from "chai";
import { Board } from "../src/Board.mjs";
import { Tetromino } from "../src/Tetromino.mjs";
/*

describe("Count empty rows", () => {
  let board;
  beforeEach(() => {
    board = new Board(10, 6);
    
  });

  it("I-tetromino.nrOfEmptyRowsLeft() returns 0", () => {
    board.drop(Tetromino.I_SHAPE);
    expect(Tetromino.I_SHAPE.nrOfEmptyRowsLeft()).to.equal(0)
  });

  it("Rotated I-tetromino.nrOfEmptyRowsLeft() returns 2", () => {
    expect(Tetromino.I_SHAPE.rotateRight().nrOfEmptyRowsLeft()).to.equal(2)
  });

  it("I-tetromino.nrOfEmptyRowsRight() returns 1", () => {
    expect(Tetromino.I_SHAPE.nrOfEmptyRowsRight()).to.equal(1)
  });

 it("O-tetromino.nrOfEmptyRowsLeft() returns 1", () => {
    expect(Tetromino.O_SHAPE.nrOfEmptyRowsLeft()).to.equal(1);
   
  });

 it("O-tetromino.nrOfEmptyRowsRight() returns 0", () => {
    expect(Tetromino.O_SHAPE.nrOfEmptyRowsRight()).to.equal(0);
  });

  it("I-tetromino has two empty bot rows", () => {
      expect(Tetromino.I_SHAPE.nrOfEmptyRows()).to.equal(2);
  })
})
*/